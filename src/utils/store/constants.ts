export const STORE_CONSTANT = {
  OVERRIDE_OPTION: "if you wish to override an existing key value use `set`",
  HAS_TYPE: "name",
};

export const METHOD = {
  SET: "set",
  SET_SAFE: "safeSet",
  GET: "get",
  MAP_GET: "mapGet",
};

export const METHOD_PARAM = {
  SET_PARAM: "name and item",
  GET_PARAM: "name",
  MAP_GET_PARAM: "name and callback function",
  HAS_PARAM: "name",
};

export const ERROR = {
  CANNOT_OVERRIDE: "cannot override existing value",
  INVALID_PARAM: "invalid parameter",
  INVALID_PARAM_TYPE: "invalid parameter type",
  MISSING_PARAM: "missing parameter",
  REQUIRES_PARAMS: "requires parameters",
  REQUIRES_PARAM: "requires parameter",
};

export const PARAM_TYPE = {
  REQUIRED_STRING: "must be a string",
  STRING: "string",
  NUMBER: "number",
};
